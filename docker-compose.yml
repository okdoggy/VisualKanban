version: "3.9"
services:
  visualkanban:
    build: .
    container_name: visualkanban
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
